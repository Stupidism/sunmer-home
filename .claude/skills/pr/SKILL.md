# Create PR Skill
1. Verify SSH connectivity: `ssh -T git@github.com` (fall back to HTTPS if needed)
2. Check current branch and diff
3. Stage changes, commit with conventional commit message
4. Push branch and create PR with description summarizing changes
5. Report exact push result — do not assume success
